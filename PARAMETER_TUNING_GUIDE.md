# 参数调整快速参考指南

## 📍 核心参数位置

### 1. **多向量权重** 
📁 文件：`src/core/engine.py` → `search()` 方法  
📍 位置：第 321-322 行

```python
text_weight=0.7,  # 文本语意（书名 + 简介）
tag_weight=0.3    # 标签语意
```

**调整建议：**
| 场景 | text_weight | tag_weight | 说明 |
|------|-------------|-----------|------|
| 当前基准 | 0.7 | 0.3 | 平衡配置 |
| 类型导向 | 0.6 | 0.4 | 用户更关注标签 |
| 内容导向 | 0.8 | 0.2 | 用户更关注故事 |
| 激进标签 | 0.5 | 0.5 | 标签和内容同等 |

---

### 2. **归一化阈值**
📁 文件：`src/core/engine.py` → `_normalize_vector_score()` 方法  
📍 位置：第 143-144 行

```python
min_threshold = 0.35  # 低于此值映射为 0.0
max_threshold = 0.65  # 高于此值映射为 1.0
```

**调整建议：**
- **放宽阈值** (0.25-0.75)：提高召回率，降低精确度
- **收紧阈值** (0.40-0.60)：提高精确度，降低召回率

---

### 3. **检索限制**
📁 文件：`src/core/engine.py` → `search()` 方法  
📍 位置：第 309 行

```python
retrieval_limit = 100  # 从 Qdrant 检索的候选数量
```

**调整建议：**
- 数据量少（< 5000）：50-100
- 数据量中（5000-50000）：100-200  
- 数据量大（> 50000）：200-500

---

### 4. **过滤阈值**
📁 文件：`src/core/engine.py` → `search()` 方法  
📍 位置：第 408 行

```python
threshold = 0.15 if self.use_multi_vector else 0.5
```

**说明：**多向量模式使用更低的阈值（0.15），因为分数是加权和而非单一相似度

---

## 🎛️ 高级调整

### 5. **负向语义强度**

当前实现：负向分数直接从总分中减去

如需调整：在 `calculate_score()` 方法中修改
```python
# 当前实现（L195）
total_score -= neg_score

# 可改为加权
total_score -= neg_score * 0.5  # 降低负向影响
```

---

### 6. **LLM Prompt 调整**
📁 文件：`src/core/llm.py`  
📍 位置：第 168-230 行 (`full_system_instruction`)

**调整点：**
- 修改 HyDE 示例，影响假设简介生成
- 调整关键词生成数量（当前 5-10 个）
- 修改过滤条件的使用说明

---

## 📊 性能监控指标

### 建议监控的指标：

1. **召回率** (Recall@K)
   - 相关结果是否被检索到
   - 调整：放宽阈值、增加 retrieval_limit

2. **精确度** (Precision@K)
   - 返回结果的相关性
   - 调整：收紧阈值、优化权重

3. **NDCG@10**
   - 排序质量
   - 调整：优化多向量权重

4. **响应时间**
   - 查询耗时
   - 调整：降低 retrieval_limit

---

## 🧪 A/B 测试建议

### 测试流程：
1. 准备 20-30 个测试查询
2. 运行基准配置（text=0.7, tag=0.3）
3. 记录结果和相关性
4. 修改参数（如 text=0.6, tag=0.4）
5. 对比结果差异
6. 选择最佳配置

### 测试查询示例：
```python
test_queries = [
    "找一本GL完結小說",  # 测试标签+过滤
    "主角聰明的科幻小說",  # 测试语义理解
    "20-50萬字的奇幻小說",  # 测试字数过滤
    "不要龍傲天的玄幻小說",  # 测试负向语义
]
```

---

## 🔧 快速调整命令

### 修改多向量权重：
```bash
# 在 src/core/engine.py 第 321-322 行
sed -i 's/text_weight=0.7/text_weight=0.65/g' src/core/engine.py
sed -i 's/tag_weight=0.3/tag_weight=0.35/g' src/core/engine.py
```

### 修改归一化阈值：
```bash
# 在 src/core/engine.py 第 143-144 行
sed -i 's/min_threshold = 0.35/min_threshold = 0.30/g' src/core/engine.py
sed -i 's/max_threshold = 0.65/max_threshold = 0.70/g' src/core/engine.py
```

---

## 📈 调优记录模板

```markdown
## 实验 X: 调整多向量权重

**日期**: 2026-03-10  
**目标**: 提升标签匹配准确度  

**参数变化**:
- text_weight: 0.7 → 0.6
- tag_weight: 0.3 → 0.4

**测试结果**:
| 查询 | 旧配置相关度 | 新配置相关度 | 改善 |
|------|-------------|-------------|------|
| GL完結小說 | 80% | 90% | ✅ +10% |
| 科幻小說 | 85% | 82% | ❌ -3% |

**结论**: 标签权重提升对类型查询有帮助，但对内容查询略有损害。建议针对查询类型动态调整。

**下一步**: 实现查询类型检测 + 动态权重
```

---

## 💡 常见问题

### Q: 为什么我的查询返回结果太少？
**A**: 可能阈值太高，尝试：
1. 降低 `min_threshold`（0.35 → 0.30）
2. 增加 `retrieval_limit`（100 → 200）
3. 降低 `threshold`（0.15 → 0.10）

### Q: 标签类型查询效果不好？
**A**: 提升标签权重：
- `tag_weight`: 0.3 → 0.4 或 0.5

### Q: 负向语义没有生效？
**A**: 检查：
1. LLM 是否正确解析（`is_negative: true`）
2. 负向分数是否计算（查看 breakdown）
3. 负向强度是否足够（可能需要乘以系数）

---

## 🎓 最佳实践

1. **小步迭代**：每次只调整 1-2 个参数
2. **记录对比**：保留每次调整的结果
3. **多样测试**：使用不同类型的查询测试
4. **用户反馈**：实际用户的满意度最重要

---

更多技术细节请参考：[SIMPLIFICATION_SUMMARY.md](./SIMPLIFICATION_SUMMARY.md)
